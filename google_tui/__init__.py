"""google-tui package."""

# Keep in sync with pyproject.toml's [project] version. Used as the fallback
# version string when the app isn't running from a git checkout (updater.py
# prefers `git describe` there, so tags win when they exist).
__version__ = "0.1.37"
